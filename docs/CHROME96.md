# Использование Chrome 96 для разработки

Этот проект настроен для тестирования в Chrome 96 без установки браузера в систему.

## Как использовать

### 🎯 Запуск

1. В первом терминале запустите dev-сервер:
   ```bash
   npm run dev
   ```

2. Во втором терминале запустите Chrome 96:
   ```bash
   npm run dev:chrome96
   ```


## Как это работает

- Скрипт использует `@puppeteer/browsers` для скачивания Chrome 96.0.4664.110
- Chrome скачивается в локальную папку `.chrome-cache` (не устанавливается в систему)
- При первом запуске скачивание может занять несколько минут (~150MB)
- При последующих запусках используется кэшированная версия
- Браузер открывается с отключенными флагами автоматизации для нормальной работы
